# Functional Docker

### Example
```
docker-compose -f <file> up      / bring all containers in file up
docker-compose -f <file> down    / bring all containers in file and other elements down

 or

docker-compose up		/ this will search current dir for 'docker-compose.yaml' and run with this
```
